# psychosis-mapping-semantic-stylemixing-interfaces

#### Setup
run docker-compose up --build --remove-orphans --force-recreate -d

Change the port number inside docker-compose.yml if it shows port is being used.
