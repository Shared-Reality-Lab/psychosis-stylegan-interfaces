# psychosis-mapping-interfaces

#### Setup
change the volume line of yml file to the correct mapping inside the container: - /absolute/path/to/app/:/app/

run docker-compose up --build --remove-orphans --force-recreate -d
